# Frontnd: React + Vite

# Backend: Express

## For frontend

run the command in terminal.

```bash
npm install && npm run dev
```

## For backend

run the command in terminal.

```bash
npm install && npm start
```
